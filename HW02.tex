\documentclass[12pt]{article}

\usepackage{subfiles}
\input{src/preamble.tex}

\title{Homework 2}

\begin{document}

\maketitle

\begin{enumerate}
    \item Given the function $f(x)=x^2$.
          \subfile{src/HW02/HW02P1.tex}

    \item Given the function:
          \begin{center}
              $
                  f(x)=
                  \begin{cases}
                      0,       & \text{if $-\pi < x \leq 0$} \\
                      \sin(x), & \text{if $0 < x \leq \pi$}
                  \end{cases}
              $
          \end{center}
          \subfile{src/HW02/HW02P2.tex}

    \item Sketch the even and odd extensions of each of the following functions, and find the Fourier cosine and sine integrals for $f$. Each function is given in the interval $0<x<\infty$.
          \begin{enumerate}
              \item $f(x)=e^{-x}$
                    \subfile{src/HW02/HW02P3a.tex}
              \item $
                        f(x)=
                        \begin{cases}
                            1, & 0<x<1 \\
                            0, & 1<x
                        \end{cases}
                    $
                    \subfile{src/HW02/HW02P3b.tex}
          \end{enumerate}
    \item Find the Fourier integral representation of the function $f_t(x)$ given below. This is sometimes called a "window" because it is "open" for $t-x < x < t+h$.
          \begin{center}
              $
                  f_t(x)=
                  \begin{cases}
                      1, & \left| x-t \right| <h \\
                      0, & \left| x-t \right| >h
                  \end{cases}
              $
          \end{center}
          \subfile{src/HW02/HW02P4.tex}
    \item Find the Fourier integral representation of each of these functions:
          \begin{enumerate}
              \addtocounter{enumii}{2}
              \item $
                        f(x)=
                        \begin{cases}
                            \left| \sin(x) \right|, & -\pi<x<\pi       \\
                            0,                      & \text{otherwise}
                        \end{cases}
                    $
                    \subfile{src/HW02/HW02P5c.tex}
          \end{enumerate}
\end{enumerate}

\end{document}