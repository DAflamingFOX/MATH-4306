% ---------------------------------------------------------
% PAGE SETUP & GEOMETRY
% ---------------------------------------------------------
\usepackage{geometry}
\geometry{letterpaper, left=15mm, right=15mm, top=15mm, bottom=20mm}

% ---------------------------------------------------------
% CORE MATH PACKAGES (Must load before unicode-math)
% ---------------------------------------------------------
\usepackage{amsmath}
% \usepackage{mathtools} % replaces amsmath
\usepackage{amsthm}
\usepackage{cancel}
\usepackage{diffcoeff}
\usepackage{xfrac}
\allowdisplaybreaks[1]

% ---------------------------------------------------------
% FONTS & TYPOGRAPHY (LuaLaTeX specific)
% ---------------------------------------------------------
\usepackage{fontspec}
\usepackage{unicode-math}

% Text font
\setmainfont{DejaVu Serif}
% Math font
\setmathfont{STIX Two Math}

% ---------------------------------------------------------
% GRAPHICS, TABLES & FIGURES
% ---------------------------------------------------------
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{float}
\usepackage{pdfpages}
\usepackage{booktabs}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

% ---------------------------------------------------------
% LISTS & TEXT FORMATTING
% ---------------------------------------------------------
\usepackage{multicol}
\usepackage{enumitem}
\setlist[enumerate, 1]{label=\textbf{\arabic*}.}
\setlist[enumerate, 2]{label=\textbf{\alph*}.}

\usepackage{titlesec}
\titleformat{\section}{\normalfont\normalsize\bfseries}{\thesection.}{1em}{}
\titleformat{\subsection}{\normalfont\normalsize\bfseries\itshape}{\thesubsection}{1em}{}

% ---------------------------------------------------------
% CROSS-REFERENCING (Always load these last)
% ---------------------------------------------------------
% \usepackage{hyperref}
\usepackage{cleveref} % Note: Must be loaded AFTER hyperref

% ---------------------------------------------------------
% BIBLIOGRAPHY
% ---------------------------------------------------------
% \usepackage[style=ieee]{biblatex}
% \addbibresource{src/refs.bib}

% ---------------------------------------------------------
% CUSTOM COMMANDS & TITLE SETUP
% ---------------------------------------------------------
\usepackage{hwtitle}

% ---------------------------------------------------------
% AUTHOR & DOCUMENT INFO
% ---------------------------------------------------------
\author{Jeffrey Morris}
\course{MATH 4306-010}
\date{\today}